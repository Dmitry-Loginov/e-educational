<form method="post" class="form-del" action="{{ path('lesson_delete', {'id': lesson.id}) }}" onsubmit="return confirm('Удалить урок?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lesson.id) }}">
    <button class="back">удалить</button>
</form>
