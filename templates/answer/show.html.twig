{% extends 'base.html.twig' %}

{% block title %}Answer{% endblock %}

{% block body %}
    <h1>Answer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ answer.id }}</td>
            </tr>
            <tr>
                <th>PathImage</th>
                <td>{{ answer.pathImage }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ answer.date ? answer.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Mark</th>
                <td>{{ answer.mark }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('answer_index') }}">back to list</a>

    <a href="{{ path('answer_edit', {'id': answer.id}) }}">edit</a>

    {{ include('answer/_delete_form.html.twig') }}
{% endblock %}
