<form method="post" class="form-del" action="{{ path('theme_delete', {'id': theme.id}) }}" onsubmit="return confirm('Удалить тему?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
    <button class="btn btn-outline-secondary">Удалить</button>
</form>
